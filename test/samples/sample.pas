program Demo;
// a Pascal program: no grammar, so Tier 2
var i: integer;
begin
  for i := 1 to 10 do
    begin
      if i > 5 then
        begin
          writeln(i); { TODO: format output }
        end;
    end;
end.
